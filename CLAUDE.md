# AlvesAssesoria

## Agent skills

### Issue tracker

As issues e PRDs vivem como issues do GitHub, gerenciadas pela CLI `gh`. Veja `docs/agents/issue-tracker.md`.

### Triage labels

Os cinco papéis canônicos de triagem, usando as strings de rótulo padrão. Veja `docs/agents/triage-labels.md`.

### Domain docs

Contexto único — um `CONTEXT.md` e `docs/adr/` na raiz do repositório. Veja `docs/agents/domain.md`.
